import React from 'react'
import ReactDOM from 'react-dom/client'
import BryptoCallEngine from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BryptoCallEngine />
  </React.StrictMode>,
)
